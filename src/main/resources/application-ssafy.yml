spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:test
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
  config:
    import: s3-local.yml, kinesis-local.yml
#logging:
#  level:
#    com.mzbr: DEBUG
#    root: DEBUG
folder:
  prefix: "encoded-video/"
